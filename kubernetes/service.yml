# service for app
apiVersion: v1
kind: Service
metadata:
  labels:
    app: app
  name: devsecops-service
  namespace:  app
spec:
  ports:
  - name: 80-3000
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: devsecops-app
  type: NodePort
status:
  loadBalancer: {}
